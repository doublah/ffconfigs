//  This file is the the initial exec of customizations, including settings, binds and aliases. This is only exec'd on game load, while userconfig.cfg is exec'd on every spawn.

// ALIASES
exec aliases.cfg

// BINDS
exec binds.cfg

// SETTINGS
exec settings.cfg
