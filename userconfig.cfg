//  This file is the reset for all customizations for classes, and should be exec'd on each class spawn to reset to default binds/aliases, before class-specific binds/aliases are used.

// ALIASES
exec aliases.cfg

// BINDS
exec binds.cfg
